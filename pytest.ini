[pytest]
markers =
    slow: exercises the real ResNet18/PennyLane pipeline (skip with -m "not slow" for a fast run)
    benchmark: measures and reports latency; not a strict pass/fail perf gate
testpaths = qknee/tests
