"""Shared observability helpers used by every UI surface (Next.js/FastAPI,
Streamlit) so a prediction's provenance is described with one vocabulary
everywhere it's shown -- see `qknee.observability.provenance`."""
