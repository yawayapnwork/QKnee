# Extra dependencies for running the test suite (qknee/tests/) — not needed
# in the production container images built by the Dockerfile.
-r requirements.txt
pytest==9.1.1
httpx==0.28.1  # required by fastapi.testclient.TestClient (qknee/tests/test_api_server.py)
